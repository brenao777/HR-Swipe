import React from 'react'

export default function FilterModal(): React.JSX.Element {
  

  return (
   <div>FilterModal</div>
  )
}


// Выпадающий список в поиске названия вакансий, format(режим работы) и schedule(занятость) с выбором в виде кнопок
